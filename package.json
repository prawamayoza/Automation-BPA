{
  "name": "bpa-automation-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "merge-report": "mochawesome-merge cypress/reports > cypress/reports/merged-report.json",
    "generate-report": "marge cypress/reports/merged-report.json -o cypress/reports --reportFilename merged-report",
    "test-and-report": "npm run test && npm run merge-report && npm run generate-report",
    "test:run": "npx cypress run || true && node cypress/scripts/generateReport.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cli-table3": "^0.6.5",
    "cypress": "^14.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
